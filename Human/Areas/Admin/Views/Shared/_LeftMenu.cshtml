
<div id="layoutSidenav_nav">
    <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
        <div class="sb-sidenav-menu">
            <div class="nav">

                <a class="nav-link @Html.Raw(ViewBag.active==0 ? "active":"")" href="~/Admin/Home/Index">
                    <div class="sb-nav-link-icon"><i class="fas fa-hourglass"></i></div>
                    HOME
                </a>
                @*<div class="sb-sidenav-menu-heading">Human</div>
                <a class="nav-link @Html.Raw(ViewBag.active==1 ? "active":"")" href="~/EmployeeList/Index">
                    <div class="sb-nav-link-icon"><i class="fas fa-address-book"></i></div>
                    Employee List
                </a>*@
                @*<div class="sb-sidenav-menu-heading">Salary</div>
                <a class="nav-link @Html.Raw(ViewBag.active==2 ? "active":"")" href="~/Admin/Salary/Index">
                    <div class="sb-nav-link-icon"><i class="fas fa-chart-area"></i></div>
                    Bảng lương
                </a>*@

                <div class="sb-sidenav-menu-heading">Leave</div>
                <a class="nav-link @Html.Raw(ViewBag.active == 3 ? "active":"")" href="~/Admin/Leave/Index">
                    <div class="sb-nav-link-icon"><i class="fas fa-chart-area"></i></div>
                    Leave 
                </a>
            </div>
        </div>
        <div class="sb-sidenav-footer">
            <div class="small">Logged in as:</div>
            @if (Session["EMAIL"] != null && Session["EMAIL"].ToString() != "")
            {
                <p>@Session["EMAIL"]</p>
            }
            else
            {
                <p>Start Bootstrap</p>
            }
        </div>
    </nav>
</div>